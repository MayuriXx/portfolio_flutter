import 'package:flutter/material.dart';

class MetaPage extends StatelessWidget {
  const MetaPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(body: Center(child: Text('Meta Page — coming soon')));
  }
}
